{
  "TikTok": {
    "AppKey": "ดูได้จาก TikTok Partner Center → App Management",
    "AppSecret": "ดูได้จาก TikTok Partner Center → App Management",
    "BaseUrl": "https://open-api.tiktokglobalshop.com"
  },

  "TikTokTenants": {
    "PoC_MobileShop_01": {
      "ShopName": "ชื่อร้านค้า",

      "AccessToken":  "ได้จาก GET /api/auth/callback → config_to_save.AccessToken",
      "RefreshToken": "ได้จาก GET /api/auth/callback → config_to_save.RefreshToken",

      "AccessTokenExpireAt":  "ได้จาก GET /api/auth/callback → config_to_save.AccessTokenExpireAt  (รูปแบบ: 2026-06-26T10:00:00Z)",
      "RefreshTokenExpireAt": "ได้จาก GET /api/auth/callback → config_to_save.RefreshTokenExpireAt (รูปแบบ: 2026-12-20T10:00:00Z)",

      "ShopCipher": "ได้จาก GET /api/auth/callback → config_to_save.ShopCipher  (= open_id จาก TikTok)",
      "ShopId":     "ได้จาก GET /api/shops/{tenantCode} → shops[].id"
    }
  }
}
